
language: java

before_script:
   # Build KITDM
   - mkdir KITDM
   - cd KITDM
   - git clone https://github.com/kit-data-manager/base
   - mvn install -DskipTests=true

script:
  mvn install -DskipTests=true
